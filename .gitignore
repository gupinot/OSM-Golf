# Dependencies
node_modules/

# Build output
dist/
build/

# Environment variables
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Claude
CLAUDE.md

# OS
.DS_Store

# IDE
.vscode/
.idea/

# Tesseract training data (large binary files)
backend/*.traineddata

# Large source assets (overlays, aerial views, PDFs) — hors git
# Ancré à la racine : ne doit pas ignorer les dossiers `data/` du code (ex. backend/src/data).
/Data/

# Cache de recherche Overpass (régénérable)
scripts/output/overpass_search_cache.json

# Cache de déploiement Firebase (régénérable)
.firebase/
